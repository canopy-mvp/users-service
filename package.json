{
  "name": "@canopy-mvp/users-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "fastify": "^4.26.0",
    "prisma": "^5.9.0",
    "bcrypt": "^5.1.0"
  }
}